node_modules/
.netlify/
.DS_Store
tests/fixtures/*.wav
*.log

# Scratch files written by review/analysis agents into the test directory.
# They are throwaway reproduction scripts, not part of the suite.
tests/_*
*.tmp.mjs
# Preview screenshots are generated at the repo root to show work in progress.
# Anchored to the root so the real artwork in src/assets/ stays tracked.
/*.png
